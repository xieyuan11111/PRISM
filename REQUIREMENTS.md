# 棱镜 PRISM — 需求文档（Requirements）

> **项目**：棱镜 PRISM — 政策与学术观点演变追踪系统
> **版本**：v0.4（开源化 + LLM 智能源选择）
> **日期**：2026-08-31
> **项目性质**：独立软件工程项目
> **关联文档**：`PROJECT.md`

---

## 1. 项目定位

### 1.1 一句话定义

**棱镜不是“新闻摘要器”，而是一个以时序图谱为核心、以原始文本为证据、以多立场讨论为解释层的“政策/观点演变追踪系统”。**

系统追踪的不是某一篇新闻本身，而是一个政策、一个学术概念或一个公共议题，如何在时间中被提出、解释、争论、修订、执行、替代或失效。

### 1.2 两类核心演变对象

#### A. 具体政策的演变

例如一项地产、医疗、教育或产业政策：

```text
问题/目标出现
  → 政策倡议或提案
  → 正式文件发布
  → 配套细则与执行口径
  → 地方/部门实施
  → 市场或社会反馈
  → 修订、补充、暂停、替代或废止
```

#### B. 学术圈观点/概念的演变

例如“消费社会中的贫困”“人工智能是否可能具有意识”“某种医疗治理模式的有效性”：

```text
问题被提出
  → 概念或理论形成
  → 不同学者提出解释
  → 争论、批评与反驳
  → 新材料/新方法进入
  → 观点修正或分化
  → 阶段性共识、未决争议或范式转换
```

### 1.3 新闻和文件的角色

新闻不是系统最终要分析的唯一对象，而是**演变过程的观测记录**。系统接收的材料包括：

- 政府及机构发布的政策原文、公告、通知、答复、会议纪要；
- 新闻报道、采访、通讯、评论和更正；
- 学术论文、书籍章节、会议发言、学者访谈与书评；
- 用户手动提供的文本、链接或文件。

每份材料都必须保留：原文、来源、发布时间、可访问 URL、材料类型和抓取时间。材料可以支持一个或多个演变节点，但**材料本身不等于节点结论**。

---

## 2. 背景与问题陈述

### 2.1 当前信息阅读的缺陷

1. **把过程压扁成结论**：新闻通常只告诉读者“发生了什么”，却不告诉读者这个政策/观点从哪里来、此前是什么状态、后来是否被修正。
2. **时间关系丢失**：发布、回应、执行、修改和废止被分散在不同日期和不同来源中，读者难以判断先后与有效期。
3. **事实与解释混杂**：政策原文、媒体叙事、利益相关方观点和分析者推断经常被混在同一段文字里。
4. **变化原因不可见**：观点改变往往不是简单“谁说服了谁”，而是新证据、新事件、新方法或制度环境改变了原有判断。
5. **单一叙事遮蔽分歧**：同一演变节点可能被监管方、产业方、受影响群体和学术观察者赋予不同含义。

### 2.2 核心问题

> 给定一组随时间积累的政策、新闻和学术材料，系统能否重建某个对象的**演变时间线、状态变化、事实关系、观点分歧和变化原因**，并让多个立场基于同一证据进行可追溯的讨论？

---

## 3. 项目目标

### 3.1 总体目标

构建一个内部使用的系统，使用户可以围绕一个“演变对象”完成：

- **材料采集**：持续采集公开政策、新闻和学术材料；
- **证据整理**：把网页、RSS、文件和用户输入标准化为可审计的 Markdown；
- **时序建模**：识别事件、实体、主张、状态、关系及其有效时间；
- **过程重建**：回答“它是怎么一步步变成现在这样的”；
- **观点追踪**：识别不同主体在不同时间对同一对象的看法及其变化；
- **多立场解释**：让多个 subagent 基于同一证据独立解释、质询和辩论；
- **用户介入**：用户可以补材料、指定目标案例和时间点、质询某一立场或要求重建某一阶段；候选筛选、冲突处理、节点修订和案例绑定由 LLM 自动完成，不要求用户逐条仲裁。

### 3.2 非目标

- ❌ 不做只输出摘要的新闻阅读器。
- ❌ 不把最新一篇报道自动当成最终事实。
- ❌ 不做替用户作出政治、投资、法律或学术裁决的“结论机器”。
- ❌ 不把预置立场包装成客观事实。
- ❌ 不做面向公众的舆情平台或实时大屏作为首期目标。
- ❌ 不采集需要绕过权限、登录墙或反爬措施才能取得的材料。

---

## 4. 核心概念与数据模型

### 4.1 演变案例（Evolution Case）

系统的最高层对象。一个案例对应一个持续演变的政策、学术观点、概念或公共议题。

示例：

```yaml
case_id: housing_policy_2026
case_type: policy
canonical_name: 2026年房地产现房销售与超长期住房贷款政策
start_at: 2026-08-01
status: active
```

`case_type` 至少支持：

- `policy`：具体政策及其制度演变；
- `academic_discourse`：学术观点、概念或理论演变；
- `public_issue`：公共议题的事实与观点演变。

### 4.2 演变节点（Evolution Node）

案例中的一个可定位阶段或变化点，不等同于一篇材料。

节点类型包括：

- `proposal`：提案/倡议；
- `draft`：草案/征求意见；
- `publication`：正式发布；
- `interpretation`：解释/解读；
- `implementation`：执行/落地；
- `response`：回应/反馈；
- `revision`：修订/补充；
- `reversal`：反转/否定；
- `replacement`：替代；
- `expiry`：到期/失效；
- `debate`：争论节点；
- `consensus`：阶段性共识；
- `open_question`：仍未解决的问题。

### 4.3 时序事实（Temporal Fact）

每条从材料中抽取的事实至少包含：

| 字段 | 含义 |
|---|---|
| `subject` | 主体/实体 |
| `predicate` | 关系或断言 |
| `object` | 客体/状态 |
| `valid_at` | 该事实开始成立的时间 |
| `invalid_at` | 该事实失效或被替代的时间，可为空 |
| `observed_at` | 材料观察到该事实的时间 |
| `source_ids` | 支撑该事实的原文材料 |
| `confidence` | 抽取/归纳置信度 |
| `provenance_type` | 原文明确写出、可靠转述、分析推断等 |

必须区分：

- **事件发生时间**；
- **文件发布时间**；
- **政策生效时间**；
- **报道时间**；
- **观点形成/发表时间**；
- **系统抓取时间**。

### 4.4 观点（Position / Claim）

观点不是“立场 subagent 的人设”，而是某个主体在特定时间对某个命题的可追踪主张：

```yaml
claim_id: claim_xxx
actor: 某机构/某学者/某媒体/某利益相关方
proposition: 该政策能够改善市场流动性
stance: support | oppose | conditional | uncertain
stated_at: 2026-08-28
based_on: [source_id_1, source_id_2]
revised_by: claim_yyy
```

观点演变要能表达：

- 谁在什么时候提出了什么主张；
- 主张依据哪些材料或前提；
- 后来是否修正、弱化、撤回或被新证据推翻；
- 不同主体的分歧是否来自事实不同、定义不同、价值目标不同或预测不同。

---

## 5. 功能需求

### FR-1 材料采集与标准化

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-1.1 | 支持 RSS/Atom、官方网页、公开新闻页、公开可访问的学术网页和本地文件导入 | P0 |
| FR-1.2 | 抓取材料输出为 Markdown，包含 YAML frontmatter、正文、来源 URL 与抓取时间 | P0 |
| FR-1.3 | frontmatter 至少包含 `source_id/title/source/published_at/fetched_at/type/case_tags` | P0 |
| FR-1.4 | 按内容指纹、规范化 URL 和来源时间去重 | P0 |
| FR-1.5 | 支持用户手动补充、修订或标注材料，并保留修改记录 | P0 |
| FR-1.6 | 采集失败可重试、记录失败原因，不以空页面替代原文 | P1 |
| FR-1.7 | 遵守公开访问边界、robots.txt、网站 ToS 与合理抓取频率 | P0 |
| FR-1.8 | 支持 Markdown 直接导入：读入 → 规范化 frontmatter → 入库 | P0 |
| FR-1.9 | 支持 PDF 导入：pdfplumber 提取文本层 → 转标准 MD → 入库；原始 PDF 留底 `raw/` | P0 |
| FR-1.10 | PDF 文本层为空或过少时自动触发 OCR（如 RapidOCR），OCR 结果转 MD 入库 | P0 |
| FR-1.11 | frontmatter 记录来源格式与处理方式：`original_format: md/pdf`、`ocr: true/false`、`extracted_via` | P0 |
| FR-1.12 | PDF 含表格时用 pdfplumber 提取表格结构 | P1 |
| FR-1.13 | 原始输入（PDF、原始网页等）统一留底 `raw/`，用于审计与重建 | P0 |
| FR-1.14 | **LLM 智能源选择**：给定议题，LLM（`source_selector` 角色）判断该采集哪些来源、优先级与相关性过滤 | P0 |
| FR-1.15 | 源选择受**白名单约束**：LLM 只能在已配置/已验证的源中选，不得凭空发明 URL | P0 |
| FR-1.16 | 源选择**可审计可仲裁**：LLM 每次建议记录理由，用户可查看、修改、覆盖 | P0 |
| FR-1.17 | LLM 不可用时回退到默认源清单（fallback），不影响基础采集 | P0 |

> **设计说明一（多格式摄入）**：棱镜 corpus 只认一种标准格式——带 frontmatter 的 Markdown。MD 和 PDF 都是"源"而非"库内格式"。摄入管线负责把一切输入转成标准 MD：PDF 先探测文本层（pdfplumber，MIT 许可），不足则走 OCR（RapidOCR，Apache-2.0）；原始文件保留在 `raw/`，保证图谱引用的证据永远能回溯到原始文件。**选型约束：PDF/OCR 库必须使用与 MIT 开源兼容的许可（MIT/Apache/BSD），避免 AGPL 传染。**

> **设计说明二（LLM 智能爬取）**：爬虫不"盲目全爬"，由 LLM 判断"该看哪些源"。但它只在**白名单内**选择，建议必须留理由，最终确认权在用户——LLM 给建议，用户拍板，杜绝乱爬与合规风险。这对应 LLM 路由层的 `source_selector` 角色（可用快速便宜的模型）。

### FR-2 文本证据库

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-2.1 | 文本库保存原始 Markdown，不把新闻正文只保存为摘要 | P0 |
| FR-2.2 | SQLite 索引材料元数据、案例关联、时间、来源、材料类型和指纹 | P0 |
| FR-2.3 | 支持 SQLite FTS5 全文检索 | P0 |
| FR-2.4 | 支持按案例、时间区间、来源、材料类型和主体过滤 | P0 |
| FR-2.5 | 每个证据片段可定位回 Markdown 文件和原文段落 | P0 |
| FR-2.6 | 可选接入项目自有语义检索服务，但必须使用项目自己的资源空间和配置 | P1 |
| FR-2.7 | 语义检索层只作为证据定位辅助，不能替代 corpus 原文与 GTI 事实 | P0 |

> **设计说明**：文本库是事实材料的可读正本；是否接入语义检索是实现选择，不改变数据边界。若材料量上升，可以接入项目自有语义检索服务；若初期规模较小，SQLite FTS5 足够。

### FR-3 GTI/Graphiti 时序图谱

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-3.1 | 棱镜使用独立 Graphiti/GTI 实例和独立 Neo4j 数据库 | P0 |
| FR-3.2 | GTI 使用项目自有的数据库、凭据、工作目录和服务边界 | P0 |
| FR-3.3 | 从材料中抽取案例、节点、实体、事实、观点和关系 | P0 |
| FR-3.4 | 所有可变化事实支持 `valid_at/invalid_at` 或等价时态表达 | P0 |
| FR-3.5 | 记录“新事实使旧事实失效/被修订/被替代”的关系 | P0 |
| FR-3.6 | 支持按历史时间点查询，而不仅查询当前状态 | P0 |
| FR-3.7 | 支持输出某案例的演变时间线，并显示每个节点的来源 | P0 |
| FR-3.8 | 支持查询变化原因：由什么事件、材料、反馈或新观点触发变化 | P0 |
| FR-3.9 | 新材料增量写入图谱，不要求每次重建全图 | P1 |
| FR-3.10 | 图谱事实必须可回溯到 corpus 的 `source_id` 和原文位置 | P0 |
| FR-3.11 | 对抽取不确定、来源冲突或时间不明的事实标记不确定性，不强行合并 | P0 |

> **设计说明（显式目标案例上下文）**：抽取默认允许模型自行判断材料归属哪个演变案例，但真实材料流中同一批材料可能被模型写成 `case: null` 或各自不同的 case_id，导致 review/synthesis 材料中合法的二手证据（`cited_prior_research`/`current_synthesis`）无法跨材料累积。因此调用方应先显式声明一个 `EvolutionCase`（CLI `process --case-id/--case-json`、`PrismAPI.process_material(target_case=...)`、`PipelineService.run_material(target_case=...)`、`ExtractionService.extract_material(target_case=...)`），LLM 只负责抽取内容并挂到该案例：目标案例的 `case_id/case_type/canonical_name/start_at/status` 不可改写，`case: null`、case_id 漂移或字段漂移抛出可审计错误而非被静默改写；case id 一律通过 CaseService/ledger 加载真实案例记录，未知 id 报错，绝不按标题、标签或向量猜测。review/synthesis 中引用的前人研究结果属于目标知识对象演变的一部分，可作为二手证据进入图谱（保留 `evidence_role`/`cited_source_ref`），但引用不等于自动 supersedes/contradicts。未声明目标案例时保持既有行为：无案例候选进入 `awaiting_case_binding`，等待显式绑定；同材料跨案例绑定继续被拒绝。

### FR-4 演变过程分析

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-4.1 | 输入案例名称，返回从起点到当前的阶段性时间线 | P0 |
| FR-4.2 | 支持查询某历史日期“当时已知什么、哪些事实当时有效” | P0 |
| FR-4.3 | 支持比较两个时间点的状态差异 | P0 |
| FR-4.4 | 区分“事实发生了变化”和“人们对事实的解释发生了变化” | P0 |
| FR-4.5 | 对政策案例识别发布、实施、调整、替代、失效链条 | P0 |
| FR-4.6 | 对学术观点案例识别提出、支持、反驳、修正、分化和阶段性共识链条 | P0 |
| FR-4.7 | 输出演变中的关键转折点、未决问题和证据缺口 | P0 |
| FR-4.8 | 允许用户指定只看政策原文、只看学术观点或只看某类主体 | P1 |

### FR-5 多立场 subagent 讨论与辩论

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-5.1 | subagent 以“分析视角/利益位置”配置，而非把预设观点当作事实 | P0 |
| FR-5.2 | 支持按案例选择立场组合，例如监管方、产业方、受影响群体、学术观察者 | P0 |
| FR-5.3 | 每个 subagent 必须先读取同一组时间线与证据，再独立输出解释 | P0 |
| FR-5.4 | 每个事实判断、因果判断和预测判断必须区分并附来源 | P0 |
| FR-5.5 | subagent 必须明确：哪些是原文事实、哪些是解释、哪些是价值判断、哪些是预测 | P0 |
| FR-5.6 | 支持围绕“变化原因”“政策效果”“观点为何改变”等问题互相质询 | P0 |
| FR-5.7 | 支持用户在讨论中指定时间点、追加材料、质询主体、要求重建某一阶段 | P0 |
| FR-5.8 | 讨论最终输出：共识、分歧、分歧来源、关键证据、未决问题和证伪条件 | P0 |
| FR-5.9 | 不要求 subagent 为了“平衡”制造对称观点；若证据明显偏向一方，应如实说明 | P0 |

### FR-6 报告与交付

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-6.1 | 输出案例演变报告 Markdown | P0 |
| FR-6.2 | 报告包含：对象定义、时间线、状态变化、关键证据、观点变化、分歧与不确定性 | P0 |
| FR-6.3 | 每个关键结论可回到材料文件、URL、发布时间和图谱事实 | P0 |
| FR-6.4 | **LLM 提炼演变总结**：由 LLM（`summarize_report` 角色）从时间线与图谱中提炼关键节点、因果链、转折点，生成"演变过程摘要"（executive summary），不是机械拼接事实 | P0 |
| FR-6.5 | 摘要必须可溯源：摘要中的每个关键判断可追溯到具体节点、材料或图谱事实，不产生无来源的新结论 | P0 |
| FR-6.6 | 支持 Markdown 转 PDF（摘要 + 完整报告） | P1 |
| FR-6.7 | 支持保存用户修正、讨论过程和最终报告版本 | P1 |
| FR-6.8 | 高信号、已人工确认的案例报告可选择性归档到项目自有分析归档空间 | P2 |

> **设计说明**：报告不是把时间线原样打印，而是由 LLM **提炼演变过程**——找出哪些节点是关键转折、什么导致了什么、分歧在何处收敛。提炼本身走 LLM 路由层（`summarize_report` 角色），但提炼出的判断必须能回溯到证据，防止 LLM 在总结里"发明"演变。

### FR-7 大模型（LLM）设置

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-7.1 | 提供独立的"模型设置"配置区（config.yaml 或设置界面），集中管理全部 LLM 相关配置 | P0 |
| FR-7.2 | 支持配置多个 provider（如方舟/硅基流动/其他 OpenAI 兼容端点），可切换、可多 provider 并存 | P0 |
| FR-7.3 | 每个 provider 配置：名称、base_url、API key、默认模型、超时、并发上限 | P0 |
| FR-7.4 | **按任务角色分派模型**：正文摘要、实体抽取、图谱构建、观点生成、辩论推理、**报告提炼**可各自指定模型（如抽取用 fast 模型、辩论/报告用 strong 模型） | P0 |
| FR-7.5 | 支持模型参数：temperature、max_tokens、top_p 等，按任务角色可调 | P1 |
| FR-7.6 | API key 只存本地 secrets 文件，不写入 corpus、图谱、日志、报告或代码仓库 | P0 |
| FR-7.7 | 支持"测试连接"：对已配置的 provider 发起一次最小请求验证可用性 | P1 |
| FR-7.8 | 模型调用失败时明确报错（限流/超时/认证失败），可重试可切换 provider | P0 |
| FR-7.9 | 支持用量统计（调用次数/token 估算），便于成本监控 | P2 |
| FR-7.10 | 界面：CLI 先行（config 文件 + 命令），如后续做 WebUI 则提供设置页 | P1 |

> **设计说明**：棱镜的每个环节都要调 LLM（爬虫后的摘要、图谱的实体抽取、辩论的推理），模型配置不能散落在各模块代码里。统一走"任务角色 → 模型"的路由层：任务定义时声明它需要的角色（extract/summarize/debate），路由层按 FR-7.4 的映射表分派到具体 provider+model。换模型只需改配置，不动代码。

### FR-8 用户界面

**设计原则**：棱镜的界面不是"看报告的工具"，而是**用户介入演变的问题与证据操作台**。核心流程由 LLM 自动完成抽取、案例归属、候选比较、冲突处理和修订；用户可以提出问题、追加证据、指定目标案例和历史时间点，但不承担逐条候选仲裁。

**形态决策**：CLI 引擎 + WebUI 操作台，同一套 Python API，两种壳分阶段交付。

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-8.1 | CLI 先行（M0-M1）：全部核心操作可脚本化——抓取、建索引、建图、发起分析、查询时间线 | P0 |
| FR-8.2 | WebUI 操作台（M2-M3）：NiceGUI 实现，纯 Python，与 CLI 共用同一 API 层 | P1 |
| FR-8.3 | **案例主页**：列出所有演变案例（类型/状态/最新节点/时间范围），支持搜索与筛选 | P1 |
| FR-8.4 | **时间线视图**：单案例演变时间线可视化（plotly），节点按时间轴排布，点击查看来源文件、当时状态、相关观点；支持指定历史时间点查看"当时已知什么" | P0 |
| FR-8.5 | **辩论剧场**：多视角 subagent 对话流实时显示；用户可随时插话、@点名某视角、追加证据、暂停/快进/重开一轮 | P0 |
| FR-8.6 | **证据库**：浏览 corpus，按来源/时间/案例/类型过滤，查看原文与原始文件（raw/） | P1 |
| FR-8.7 | **摄入入口**：拖拽上传 MD/PDF，实时显示处理状态（转文本/OCR 中/入库完成/失败原因） | P1 |
| FR-8.8 | **模型设置页**：多 provider 配置、按任务角色分派模型、测试连接、用量统计 | P1 |
| FR-8.9 | 所有 WebUI 页面只访问**项目自有数据**（不读写项目外部系统），数据修改操作（追加证据、触发 LLM 重算）权限受控、留审计记录；界面配置（端口、主题、权限）独立管理 | P1 |
| FR-8.10 | WebUI 与 CLI 输出一致：同一案例在两种界面看到相同的时间线与证据 | P0 |

> **设计说明**：CLI 是引擎的"后台"，WebUI 是"操作台"，二者不重复实现逻辑。辩论剧场的核心是多个 LLM 视角基于同一证据自动解释、质询和收敛；用户可以在过程中追加材料、指定问题或目标案例，但不需要逐条审核候选。

---

## 6. 系统架构与数据流

```text
公开材料 / 用户材料（Markdown / PDF / 网页 / 手动文本）
        │
        ▼
摄入层（Ingestion）
        │  PDF→文本(pdfplumber) → 扫描件→OCR(RapidOCR)
        │  Markdown→规范化；网页→正文提取
        │  统一输出标准 MD + frontmatter（含 original_format/ocr 标记）
        │  原始文件留底到 raw/
        ▼
文本证据库（corpus + SQLite FTS5）
        │
        ├── 原文定位 / 语义检索（可选项目自有服务）
        │
        ▼
GTI/Graphiti 时序图谱（棱镜独立实例）
        │  案例、节点、事实、观点、时间、失效/修订关系
        ▼
演变过程查询器
        │  时间线、历史状态、转折点、变化原因、证据缺口
        ▼
多立场讨论层
        │  独立解释 → 交叉质询 → 反驳 → 用户介入 → 收敛
        ▼
演变报告（Markdown → PDF）
        │
        └────────── 大模型服务层（LLM Router）──────────
              provider 1（如方舟）  provider 2（如硅基流动） …
              按任务角色分派：extract / summarize / debate
              API key 存本地 secrets，不入库不入报告
```

### 数据边界

- `raw/` 是原始输入留底（PDF、原始网页等），用于审计与重建。
- `corpus/` 是标准化后的材料事实源，必须可读、可备份、可重建。
- SQLite 是材料索引，不是最终事实裁判。
- GTI 是演变关系与时态事实层。
- 语义检索服务（如果接入）只服务棱镜自己的材料和索引。
- 大模型服务层通过配置路由，密钥只在本地 secrets 文件。
- 棱镜的图谱、文本与报告只在项目边界内流转。

---

## 7. 典型使用场景

### U1：重建一项具体政策的演变

用户输入：“追踪 2026 年某地产政策从提出到现在的变化。”

系统应返回：

1. 最初倡议/传闻与正式文件的区别；
2. 正式发布日、生效日和执行范围；
3. 配套细则、部门解释和地方实施差异；
4. 市场/社会反馈及其证据；
5. 后续修订、弱化、替代或失效情况；
6. 当前状态与仍未确认的部分。

### U2：查询历史时点状态

用户输入：“在 8 月 20 日，当时这项政策已经生效了吗？”

系统必须按时间点过滤事实，不能用今天的状态倒灌回历史。

### U3：追踪一个学术观点如何改变

用户输入：“追踪‘消费社会中的穷人’这个观点从早期工作伦理解释到鲍曼式消费者解释的变化。”

系统应区分：

- 不同时期的概念定义；
- 代表性文本与提出者；
- 观点之间的继承、批评和修正；
- 新经验材料如何改变解释框架；
- 当前仍存在的理论分歧。

### U4：多立场解释变化原因

用户输入：“为什么这项政策从鼓励转向限制？不同主体会怎么解释？”

subagent 应分别从制度目标、执行成本、产业利益、受影响群体和学术观察等角度解释，所有解释必须回到时间线和材料证据。

### U5：用户纠正与追加

用户说：“这篇报道把征求意见稿当成正式政策了，我补一份原文。”

系统应：

1. 把新材料存入 corpus；
2. 标记旧材料的事实等级或来源问题；
3. 更新相关 GTI 节点与有效时间；
4. 重新生成受影响的时间线和讨论结论；
5. 保留修正前后的版本差异。

---

## 8. 非功能需求

### NFR-1 时序正确性（最高优先级）

- 不能用后来的材料无标记地改写历史状态。
- 每条事实必须区分发生、发布、生效、观察和失效时间。
- 新事实与旧事实冲突时，保留冲突与来源，不静默覆盖。
- 支持“截至某日”的历史查询。

### NFR-2 证据可追溯

- 关键事实、观点和结论必须有 `source_ids`。
- 来源必须能回到真实材料文件和原文位置。
- 报告中区分原文明确陈述、可靠转述、模型归纳和模型推断。

### NFR-3 隔离性

- 棱镜 GTI 使用独立 Neo4j 数据库、配置、凭据、服务和备份。
- 棱镜 corpus、图谱和报告只使用项目自有数据源。
- 棱镜 subagent 不读取项目外的私人记忆来填补证据缺口。

### NFR-4 可重建性

- corpus 是底层材料正本。
- SQLite 索引、语义检索索引和 GTI 图谱都应可从 corpus 重建或校验。
- 每次图谱写入保留批次和来源信息。

### NFR-5 可靠性与性能

- 增量采集失败不影响既有案例。
- 单案例时间线查询目标 ≤ 10 秒（不含 LLM 长篇解释）。
- 3-4 个 subagent 的一轮讨论目标 ≤ 10 分钟。
- LLM、爬虫和图谱服务失败时返回明确错误，不生成伪造结论。

### NFR-6 合规与隐私

- 只采集公开、允许访问的材料。
- 不绕过登录墙、付费墙或技术访问控制。
- API key、Cookie 和个人凭据只放本地 secrets，不进入 corpus、图谱、日志和报告。
- 内部分析和公开发布使用不同输出边界；公开材料需再次人工审阅。

---

## 9. 里程碑与最小可用产品

### M0：单案例演变闭环（先不做完整辩论）

目标：证明系统能把材料变成一条可回溯的演变时间线。

- [ ] 选择一个具体政策案例或一个学术观点案例；
- [ ] 接入 2-3 个合规公开来源；
- [ ] 材料标准化为 Markdown + frontmatter；
- [ ] SQLite 索引和 FTS5 可用；
- [ ] 棱镜独立 GTI 实例完成最小 schema；
- [ ] 至少写入 5 个带时间和来源的演变节点；
- [ ] 能回答“截至某日，案例处于什么状态”；
- [ ] 输出第一份带证据链的演变报告。

### M1：增量与修订

- [ ] 新材料能增量写入；
- [ ] 支持旧事实失效/被修订/被替代；
- [ ] 支持来源冲突标记；
- [ ] LLM 自动纠正节点并保留原始抽取与修订版本差异；
- [ ] 可选接入项目自有语义检索服务做语义定位。

### M2：多立场解释

- [x] 配置 3-4 个分析视角；
- [x] 所有 subagent 读取同一时间线和证据集；
- [x] 输出事实/解释/价值判断/预测的区分；
- [x] 支持一轮相互质询；
- [x] 输出共识、分歧、分歧来源和未决问题。

> **M2 验收记录（2026-09-04）**：CLI/API 自动辩论已完成独立解释、
> 一轮交叉质询和证据绑定综合。真实 provider smoke 已分别通过
> `academic-hnad-evolution`（学术视角组）和
> `china-housing-provident-fund`（通用政策视角组）。用户不逐条仲裁；
> 候选筛选、冲突处理和综合由 LLM 自动完成，确定性层负责证据、时间和
> 引用边界。异常 provider 输出保守降级并留审计。

### M3：用户互动与报告

- [ ] 用户可在讨论中追加材料；
- [ ] 用户可指定历史时间点和阶段；
- [ ] 用户可点名主体或 subagent 追问；
- [ ] 支持报告版本化与 PDF 导出；
- [ ] 支持多个并行演变案例。

---

## 10. 质量验收标准

### 时序验收

- [ ] 至少一个案例能正确区分“提出、发布、生效、修订、失效”；
- [ ] 历史时间点查询不使用未来材料覆盖当时状态；
- [ ] 新事实替代旧事实时，旧事实仍可追踪。

### 证据验收

- [ ] 关键节点 `source_ids` 覆盖率 100%；
- [ ] 报告中的关键事实可定位到原文文件和段落；
- [ ] 冲突来源不会被静默合并。

### 讨论验收

- [ ] subagent 不能把自己的立场当作事实；
- [ ] 事实、解释、价值判断和预测分别标注；
- [ ] 每个重要观点都有来源或明确标注“证据不足”；
- [ ] 用户追加证据后，受影响的时间线和结论能重算。

### 隔离验收

- [ ] 棱镜 GTI 使用项目自有的数据库、服务和凭据；
- [ ] 棱镜材料只出现在项目自有的文本/索引/图谱中；
- [ ] 棱镜报告不自动写入项目外部系统。

---

## 11. 初始立场/分析视角设计

这里的“立场”是**观察位置**，不是预先规定答案：

1. **制度/监管视角**：关注政策目标、合法性、执行能力、治理成本与副作用；
2. **产业/执行视角**：关注可操作性、激励、成本、市场反馈与替代方案；
3. **受影响群体视角**：关注分配影响、实际体验、机会成本与风险转移；
4. **学术观察视角**：关注概念定义、因果链、证据质量、历史比较与解释边界。

针对学术观点案例，可以把视角替换为不同理论传统、方法论或学派，但必须明确“这是分析镜头，不是系统已证实的事实”。

---

## 12. 待定事项

1. M0 先做哪类案例：具体政策，还是学术观点演变？
2. 首个案例是否使用“现房销售/超长期住房贷款”等近期地产政策？
3. 首批政策来源和学术来源清单；
4. GTI 的 Neo4j 部署位置、独立数据库名和备份策略；
5. 是否在 M1 接入项目自有语义检索服务，还是 M0 先使用 MD + SQLite；
6. 用户互动先做 CLI，还是直接做 WebUI 对话界面。
7. 是否从一开始就公开到 GitHub，还是先本地跑通 M0 再开源（当前倾向：先跑通再开源，但代码标准从第一天按开源写）。

---

## 13. 开源化设计（2026-08-31 新增）

棱镜计划作为**开源软件项目**发布到 GitHub。这改变了部分设计约束。

### 13.1 许可证与治理

- 首选 **MIT 许可证**（宽松、社区友好、与常见生态一致）。
- 仓库需包含：`LICENSE`、`README.md`、`CONTRIBUTING.md`、`CHANGELOG.md`、`CODE_OF_CONDUCT.md`。
- 示例配置全部用**占位符**（`your-api-key`），真实密钥只存在于本地 `secrets.env`（.gitignore 排除）。

### 13.2 可移植性（去掉本机假设）

- 所有路径使用**项目相对路径**，根目录可通过环境变量（如 `PRISM_HOME`）或 config 指定，不硬编码绝对路径。
- 不假设特定操作系统或预装服务；Neo4j/Graphiti 提供 **Docker Compose** 一键部署。
- 依赖清单化（`pyproject.toml` + lock），README 写明安装步骤与系统要求。
- LLM provider 配置模板化：示例用通用 OpenAI 兼容端点占位，不绑定任何特定厂商。

### 13.3 插件化设计（贡献者生态）

开源项目的生命力在贡献者生态，棱镜的扩展点全部做成插件：

- **数据源插件**：新爬虫源 = 实现一个接口（RSS/页面/自定义），注册即可用，无需改核心。
- **分析视角插件**：新立场/视角 = 一个 persona 配置 + 可选逻辑，可独立贡献。
- **摄入格式插件**：新文件格式 = 实现"转标准 MD"接口（如未来加 docx/html）。
- **LLM provider 插件**：新增 provider = 一个 OpenAI 兼容配置项（或自定义适配器）。

### 13.4 开源后的项目边界表述

- 文档不再提及任何私人记忆系统、个人名字或本机服务作为依赖。
- 项目自我表述为：**纯自托管、无外部私有依赖、各层可替换**的开源软件。
- 内部使用与开源版本共用同一套代码库；敏感配置只在本地，不入仓库。

### 13.5 开发节奏

- **代码标准从第一天按开源写**（相对路径、可配置、无硬编码密钥），避免跑通后再重构。
- 但**发布时机先本地**：M0 跑通、文档齐全、无残留本机信息后，再公开到 GitHub。
- 首次发布前需做一次"开源审查"：扫描仓库内有无残留路径/密钥/个人引用。

### 13.6 测试与 CI

- 核心模块（摄入、索引、图谱写入、LLM 路由）必须配套单元测试。
- 提供 `pytest` 测试套件，随代码仓库分发。
- 开源后配置 GitHub Actions 基础 CI：安装依赖 → 跑测试 → 静态检查（ruff）。
- 不在 CI 中调用真实 LLM/爬虫（避免费用与反爬）；用 mock/fixture 数据测试。

### 13.7 安全边界

- WebUI 默认仅绑定 `127.0.0.1`（本机访问）。
- 如需远程/局域网访问，由用户自行配置反向代理或 Tailscale，项目不主动暴露端口。
- 模型 API key 只存本地 secrets，WebUI 设置页不展示明文密钥。
- 提交安全漏洞请走仓库的私有披露渠道（CONTRIBUTING 中说明）。

---

## 14. 核心判断

棱镜真正要保存的不是“今天发生了一条新闻”，而是：

> **一个对象在时间里经历了什么变化；谁在什么时刻如何描述它；哪些事实使旧判断失效；不同的人为什么从同一过程里看出了不同的意义。**

因此：

- corpus 保存材料；
- SQLite 管索引；
- GTI 保存演变关系与事实时态；
- subagent 解释和争论演变；
- 用户负责提出问题、补充证据与判断哪些解释值得保留。

---

*本文档为 PRISM 项目需求基线 v0.4。*
