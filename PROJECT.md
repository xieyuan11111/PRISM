# 棱镜 PRISM — 政策与学术观点演变追踪系统

> **项目代号**：棱镜 PRISM（定稿 2026-08-31）
> **项目性质**：独立软件工程项目
> **日期**：2026-08-31
> **方法论内核**：时序事实追踪（GTI/Graphiti）+ 可追溯文本证据 + 多视角解释与辩论

---

## 1. 一句话定位

**持续采集政策、新闻与学术材料，以 GTI 重建政策或观点的演变过程，再由多个分析视角基于同一时间线与证据解释变化原因，并允许用户深度介入。**

新闻不是最终分析对象，而是演变过程的观测材料。系统的核心对象是“演变案例”：既可以是一项具体政策从提出、发布、实施到修订/失效的过程，也可以是一个学术观点从形成、争论到修正、分化或形成阶段性共识的过程。

## 2. 为什么值得做

- 政策和学术观点都不是静态结论，而是**在时间中不断变化的对象**。
- 单篇新闻或单篇论文只能记录一个截面，无法回答“此前是什么、后来为什么改变、现在什么仍然成立”。
- GTI 原生支持 `valid_at/invalid_at`、历史时间点查询和新事实使旧事实失效，适合保存演变而不是堆积材料。
- 多视角讨论的价值不是制造对称立场，而是解释：不同主体为什么从同一演变过程里得出不同判断，以及分歧来自事实、定义、目标还是预测。
- 这是“政策演变进程/学术观点改变过程”的正式工程化实现。

## 3. 系统架构（四大模块）

```
                    ┌─────────────────────────────┐
                    │        辩论剧场 (Debate)     │
                    │  立场A ──┐      ┌── 立场B    │
                    │  立场C ──┴──┬──┴── 立场D    │
                    │    观众=用户（可随时插话）   │
                    └──────────┬──────────────────┘
                               │ 观点必须有引文
                    ┌──────────▼──────────────────┐
                    │  时序图谱 (GTI/Graphiti)     │
                    │  事件×时间×实体×来源          │
                    └──────────┬──────────────────┘
                    ┌──────────▼──────────────────┐
                    │    文本库 (Corpus)           │
                    │  MD文件 + SQLite索引          │
                    └──────────┬──────────────────┘
                    ┌──────────▼──────────────────┐
                    │    爬虫 (Crawlers)           │
                    │  官方政策源 → 新闻聚合源      │
                    └─────────────────────────────┘
```

### 3.1 爬虫层（Crawlers）

**原则：官方源优先，政策文件比新闻好爬。**

- **第一梯队（政策）**：政府网站/政策发布页，更新慢、结构固定、多有 RSS。用 RSS + 页面解析。
- **第二梯队（新闻）**：新闻聚合/特定栏目，结构较乱，需正文提取。
- **网络访问**：按数据源配置直连或代理，遵守访问频率和站点规则。
- 反爬：低频、守规矩、单源单线程，宁可慢不可封。

**输出**：标准化 MD 文件 → `corpus/YYYY-MM/来源/标题.md`，带 YAML frontmatter（日期、来源、URL、类别、正文）。

### 3.2 文本库（Corpus）

**决策：MD 文件 + SQLite 索引为基础，不强制引入额外语义数据库**（理由：文本正本应保持可读、可审计、可重建）。

- `corpus/`：原始 MD 文件，按日期+来源分目录。
- `index.db`（SQLite）：文件路径、标题、日期、来源、类别、摘要、关键词。支持按时间/来源/关键词过滤。
- 高信号结论可在后续版本归档到项目自己的分析归档区。

### 3.3 时序图谱（GTI/Graphiti）— 定稿 2026-08-31

**决策：用 Graphiti（GTI）独立实例，为棱镜项目专用。**

- 棱镜使用**独立的 Graphiti 环境、Neo4j 数据库、配置、凭据、工作目录和服务边界**。
- 复用时序图谱能力：事件-时间-实体-关系，天然支持"2026年8月地产新政完整时间线"这类查询。
- 实体：机构、人物、政策名、事件、地点。关系：`发布`、`回应`、`导致`、`引用`、`反对`、`关联`。
- 时间：每条记录带 `published_at` / `effective_at`。
- 图谱基建由项目自行部署和维护，使用独立数据库。

### 3.4 辩论剧场（Debate）— 全案灵魂

**使用独立的多代理编排层**：spawn 3-4 个子代理，各注入一个分析视角，共享同一批知识库证据。

- 每个 subagent 遵循**证据优先方法论**：观点必须带引文（`[corpus/...]`），不许无据断言；区分事实/解释/价值判断/预测。
- 流程：独立观点输出 → 互相质询（可翻原文）→ 反驳与辩护 → 收敛出"分歧清单 + 共识点"。
- **用户 = 仲裁者**：随时可插话、点名某个立场、追加证据、打断辩论——这是和"纯自动辩论"最大的区别，也是最有价值的部分。
- 立场配置要**可插拔**：`config.yaml` 里定义分析视角清单（如：制度/监管、产业/执行、受影响群体、学术观察），按议题选组合。

## 4. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | **Python 3.11（全栈单语言）** | 性能瓶颈在 LLM/数据库，不在语言；单语言降低维护成本 |
| 爬虫 | Scrapy + feedparser（需 JS 时 Crawlee/Playwright） | RSS 优先，官方政策源静态页最稳 |
| 摄入 | **pdfplumber + RapidOCR** | PDF 文本/表格提取 / 扫描件 OCR；**均为 MIT/Apache 兼容许可，无 AGPL 风险** |
| 存储 | MD 文件 + SQLite（FTS5） | 简单、可读、可审计；量大后可升 Meilisearch（Rust） |
| 图谱 | **Graphiti（GTI）独立实例**（定稿） | 时间感知、事实失效、可溯源，正中"演变"需求 |
| 辩论 | AutoGen/AG2（备选 LangGraph） | 原生多代理辩论 + 人类介入 |
| 界面 | CLI 先行 + NiceGUI WebUI 操作台 | 同 API 双壳，辩论剧场可实时介入 |
| 配置 | config.yaml + secrets（本地） | 立场/源/阈值/模型全部可配，密钥不入库 |

## 4.1 实现语言决策（为什么全 Python）

棱镜的性能真实分布：

```
LLM 调用（摘要/抽取/辩论推理）  ≈ 90%+   ← 网络 IO，与语言无关
OCR / PDF 提取                  ≈ 5%     ← 已是 C 库（onnxruntime / pdfplumber 底层）
SQLite / 图谱查询               ≈ 3%     ← 已是 C 库 / 独立数据库服务（Neo4j=Java、FalkorDB=C）
爬虫抓取                        ≈ 2%     ← 网络 IO + 低频政策源
```

- 为了 2-5% 的提升引入第二门语言，会让 90% 的维护成本翻倍——**不划算**。
- 全 Python 单语言 + 高性能底层库（C/Rust 绑定 + 独立数据库服务）= 性价比最优。
- 唯一保留的异构可能：若未来需要全网超高并发抓取，单独用 Go(colly) 写爬虫服务，通过 HTTP/消息队列与棱镜解耦。**M0-M3 均不需要。**

## 4.2 耦合架构（"真正耦合"靠什么实现）

语言统一是必要条件，真正耦合靠四件事：

### ① 共享领域模型（Shared Domain Schema）

所有层共用一套数据契约，定义在 `src/domain/`：

```python
Material        # 材料（爬虫/摄入产出）：id, title, source, published_at, fetched_at, original_format, ocr, content, raw_path
EvolutionCase   # 演变案例：id, case_type, canonical_name, start_at, status
EvolutionNode   # 演变节点：id, case_id, node_type, happened_at, summary, source_ids
TemporalFact    # 时序事实：subject, predicate, object, valid_at, invalid_at, observed_at, source_ids, confidence, provenance_type
Claim           # 观点：id, actor, proposition, stance, stated_at, based_on, revised_by
```

爬虫产出 Material，摄入转标准 MD，图谱抽 TemporalFact，辩论读 Claim——**各层不各自发明数据结构，都 import 同一个 schema**。

### ② 事件驱动，不是文件轮询

层与层之间不靠"定时扫目录"连接，靠事件总线（**asyncio 进程内事件**，M0-M3 够用；跨进程场景后续可按需引入 ZeroMQ）：

```
摄入完成 → emit("material.ingested", material_id)
  → 索引监听 → 更新 SQLite + 生成摘要
  → 图谱监听 → 增量抽取实体关系（Graphiti）
  → 辩论监听 → 提示"新证据可用，可追加到活跃案例"
```

新材料进来，全链路自动联动，不需要人手动跑三步脚本。

### ③ 单一 API 层（Facade）

CLI 和 WebUI 都只调 `src/api/` 暴露的同一组服务函数：

```python
ingest_material(path)      # 摄入（MD/PDF/OCR 自动路由）
build_timeline(case_id)    # 时间线查询
start_debate(case_id, perspectives)  # 发起辩论
query_history(case_id, as_of)        # 历史时点查询
```

层与层之间不互相直接调函数，都走 API 层——可替换、可测试、可审计。

### ④ 数据库即契约（DB as Contract）

SQLite schema 是层间契约：corpus 表、图谱映射表、事件日志表。任何一层故障，其他层通过 DB 仍能读到一致状态。

### 耦合示例：拖入一份扫描版政策 PDF

1. 摄入层 → pdfplumber 探测 → 文本层空 → **自动触发 RapidOCR**（无需人干预）
2. OCR 完成 → emit `material.ingested` → 存 corpus（`original_format: pdf, ocr: true`）+ 原始文件留底 raw/
3. 索引监听 → 更新 SQLite + 生成摘要（LLM 路由，extract 角色）
4. 图谱监听 → 增量抽取实体/关系/时间 → Graphiti
5. 辩论层收到通知 → 若材料属于活跃案例，提示"新证据已入库"
6. WebUI 显示状态实时流转：`OCR中 → 入库 → 已索引 → 已入图`

这就是"真正耦合"——不是四个独立工具拼起来，是一个事件驱动的整体。

## 5. 目录结构（草案）

```
PRISM_HOME/                 # 项目根目录（默认 ./，可用环境变量 PRISM_HOME 指定）
├── PROJECT.md              # 本文档
├── REQUIREMENTS.md         # 需求文档
├── OPENSOURCE-RESEARCH.md  # 开源选型调研
├── config.yaml             # 立场、数据源、阈值、模型
├── secrets.env             # API key（gitignore，不入库）
├── corpus/                 # 标准化文本库（摄入输出）
│   └── 2026-08/来源/标题.md
├── raw/                    # 原始输入留底（PDF/原始网页）
├── src/
│   ├── domain/             # 共享领域模型（Material/Case/Node/Fact/Claim）
│   ├── events/             # 事件总线（asyncio 进程内）
│   ├── api/                # 单一 API 层（CLI/WebUI 共用）
│   ├── crawlers/           # 爬虫（每源一个模块）
│   ├── ingestion/          # 摄入：MD 规范化 / PDF 提取 / OCR 路由
│   ├── store/              # SQLite 索引 + FTS5
│   ├── graph/              # Graphiti 图谱构建与查询（独立实例）
│   ├── llm/                # LLM 路由层（角色→模型映射，多 provider）
│   ├── debate/             # 辩论编排（AutoGen，视角 persona）
│   └── webui/              # NiceGUI 操作台
├── scripts/
│   ├── crawl_all.py        # 一键抓取
│   ├── ingest_files.py     # 导入本地 MD/PDF
│   ├── build_index.py      # 重建索引
│   ├── build_graph.py      # 重建图谱（Graphiti）
│   ├── run_debate.py       # 发起一场辩论
│   └── serve_webui.py      # 启动 WebUI 操作台
└── tests/
```

## 5.1 开源化（2026-08-31）

棱镜计划作为**开源项目**发布到 GitHub（MIT 许可，先本地跑通 M0 再公开，但代码标准从第一天按开源写）。

- **可移植**：路径用项目相对路径 + `PRISM_HOME` 环境变量，不硬编码绝对路径；Neo4j/Graphiti 提供 Docker Compose 一键部署；LLM provider 模板化，示例用占位符。
- **插件化**：数据源、分析视角、摄入格式、LLM provider 全部做成可插拔接口，贡献者无需改核心即可扩展。
- **无残留**：示例配置全占位符，真实密钥只在本机 `secrets.env`（.gitignore 排除）；发布前做一次"开源审查"扫描残留路径/密钥/个人引用。
- **文档边界**：不提及任何私人记忆系统或外部 Agent 框架作为依赖，自我表述为纯自托管开源软件。

## 5.2 LLM 智能源选择

爬虫不盲目全爬，由 LLM（`source_selector` 角色）判断"该看哪些源"：

- 给定议题 → LLM 建议源清单 + 优先级 + 理由
- **白名单约束**：只能在已配置/已验证的源中选择，不得凭空发明 URL
- **用户仲裁**：建议可查看、可修改、可覆盖，最终确认权在用户
- **fallback**：LLM 不可用时回退默认源清单

## 6. 数据流（端到端）

1. `crawl_all.py` → 抓取新政策/新闻 → 写入 `corpus/`（带 frontmatter）
2. `build_index.py` → 扫描 corpus → 更新 SQLite 索引（含摘要）
3. `build_graph.py` → 从索引抽实体/关系/时间 → 写入 Graphiti 独立实例
4. `run_debate.py --topic "2026年8月地产新政"` → 从图谱+文本库取证据 → spawn N 个立场 subagent → 辩论 → 输出报告（分歧/共识/证据链）
5. `summarize_report.py` → LLM（`summarize_report` 角色）从时间线/图谱提炼关键节点、因果链、转折点 → 生成"演变过程摘要" → 摘要+完整报告转 PDF

## 7. 里程碑

### M0 — 最小闭环（先跑通，2-3 个源）
- 爬 2-3 个官方政策源（RSS）→ corpus
- SQLite 索引 + 简单搜索
- **单立场**分析（一个 subagent 基于语料出观点报告，带引文）
- ✅ 验收：对"地产新政"出第一份可用的单立场分析

### M1 — 时序图谱（Graphiti 独立实例）
- 搭建棱镜专用 Graphiti 环境（独立 Neo4j DB）
- 事件-时间-实体入库，支持时间线查询

### M2 — 多立场辩论
- config.yaml 立场清单
- delegate 多 subagent 辩论编排
- 用户插话/仲裁界面（CLI）

### M3 — 打磨
- 源扩充、正文提取强化、反爬策略、报告生成（md2pdf 复用）

## 8. 风险与边界（诚实清单）

1. **政治敏感**：多立场辩论涉及敏感议题时，输出定位是"立场+证据链呈现"，不是"事实判决"。内部工具可用，公开传播要蒸馏。
2. **立场退化成复读机**：若 subagent 只输出预置观点、不引用证据，辩论就是自说自话。必须强制引文。
3. **图谱质量**：实体抽取依赖 LLM，小语料下可能稀疏。M1 先看效果再决定策略。
4. **爬虫合规**：官方源 + 低频 + 只存公开文本，遵守 robots 与 ToS。
5. **项目边界**：棱镜的代码、数据、图谱、配置和运行环境全部独立管理。

## 9. 决策记录（定稿项）

| 决策 | 定稿 | 日期 |
|---|---|---|
| 项目名 | **棱镜 PRISM** | 2026-08-31 |
| 文本库 | 独立 MD + SQLite | 2026-08-31 |
| 图谱 | **Graphiti（GTI）独立实例** | 2026-08-31 |
| 语言 | **全 Python 3.11 单语言** | 2026-08-31 |
| 耦合架构 | 共享 schema + 事件驱动 + 单一 API 层 | 2026-08-31 |
| 界面 | CLI 先行 + NiceGUI WebUI | 2026-08-31 |
| 开源 | **MIT，GitHub 发布，先本地跑通再开源** | 2026-08-31 |
| 智能爬取 | **LLM 判断源选择（白名单 + 用户仲裁）** | 2026-08-31 |
| 首批数据源 | 待定（政策类优先） | — |
| 辩论立场 | 待定（按议题可插拔） | — |

---

*本文档为 PRISM 项目奠基文件。*
